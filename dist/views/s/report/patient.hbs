
<div class="container">
    <div class="row">
        <div class="col-12 d-flex justify-content-between mb-3">
            <h3>Reportes Pacientes</h3>
            {{#if report}}
                <a href="{{report}}" target="_blank" class="btn btn-danger">
                    <i class="bi bi-pdf"> Descargar Reporte</i>
                </a>
            {{/if}}
        </div>
    </div>
    <div class="row">
        <form class="card" target="_blank" action="?generate=true">
            <div class="card-body">
                <label>Dirección</label>
                <select name="address" class="form-control">
                    <option selected></option>
                    {{#each addressList}}
                        <option value="{{this.id}}">{{this.description}}</option>
                    {{/each}}
                </select>

                <label>Pacientes</label>
                <select name="patient" class="form-control">
                    <option selected></option>
                    {{#each patientList}}
                        <option value="{{this.id}}">{{this.name}} {{this.lastname}}</option>
                    {{/each}}
                </select>

                <button type="submit" target="_blank" class="btn btn-primary mt-3">Generar</button>
            </div>
        </form>
    </div>
</div>
