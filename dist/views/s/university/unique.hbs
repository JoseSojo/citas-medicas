
<div class="card">
    <div class="card-body py-0 pt-2 d-flex justify-content-between">
        <h3>{{data.name}}</h3>
        <ul class="d-flex gap-4">
            <li style="list-style: none;">
                <a href="/speciality" class="btn btn-primary">
                    lista
                    <i class="bi bi-card-list"></i>
                </a>
            </li>
            <li style="list-style: none;">
                {{!-- <button class="btn btn-primary d-flex gap-2" data-toggle="modal" data-target="#modalCreateSpeciality">
                    actualizar
                    <i class="bi bi-pen-fill"></i>
                </button> --}}
            </li>
            {{#isAdmin user}}
                <li style="list-style: none;">
                    <button class="btn btn-danger d-flex gap-2" data-toggle="modal" data-target="#deleteObject">
                        Eliminar
                        <i class="bi bi-trash-fill pt-1"></i>
                    </button>
                </li>
            {{/isAdmin}}
        </ul>
    </div>
</div>

<div class="card mby-3">
    <div class="card-body">
        
        <p class="lead">{{data.description}}</p>

        <ul>
            <li>Especialistas: {{data._count.doctors}}</li>
            <li>Cirujanos: {{data._count.user}}</li>
            <li>Creador: {{data.createReference.name}} {{data.createReference.lastname}}</li>
        </ul>

    </div>
</div>
